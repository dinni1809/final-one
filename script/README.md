# Project Scaffold Scripts

Run this from the project root to create the backend folders and starter files described in `structure.md`:

```bash
node script/generate-structure.js
```

The script creates missing files only. Existing files are skipped so local edits are not overwritten.
